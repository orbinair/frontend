import { Preview, Meta, Story, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, select, number, color } from "@storybook/addon-knobs";
import Button from './Button';

<Meta title="buttons/Button" component={Button} decorators={[withKnobs]} />

# Button

Basic button component.

<Preview>
  <Story name="Sandbox">
    <Button
      className={text('className', '')}
    >
      {text('text', 'Button text')}
    </Button>
  </Story>
</Preview>

<Props of={Button} />
